# DELIVER

Produce `DELIVER.md` and leave the selected usable work product in place.

Record `deliver`, `hold`, or `kill`; the selected MAKE candidate; its recovery reference; exact work-product paths; the shortest launch or use instructions; references to that candidate's CHECK and ALIGN records; concise limitations; and exact blockers.

Use `deliver` for a useful aligned result with independently produced CHECK evidence, `hold` when a material criterion is blocked or an authorized external step remains, and `kill` when no useful candidate survives. Lead the final response with the result and its usage.
